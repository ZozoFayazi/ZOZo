<analysis>**original_problem_statement:** The user's goal is to prepare the ZOZO Burger application for go-live. This involves moving from the  POS system to a new, custom-built system called .

Key tasks included:
1.  Fixing various UI/data bugs in the admin panel (incorrect category display, duplicate products).
2.  Implementing a drag and drop feature for reordering products.
3.  Correcting product pricing logic to handle different sizes and single-size items.
4.  Implementing a robust POS connector for the new  system.
5.  Guiding a parallel agent to build the  system itself, evolving it from a simple service to a multi-tenant SaaS with a guided in-store ordering UI.
6.  Creating a final project summary document.
7.  Implementing a critical POS order retry mechanism to prevent lost sales.

**PRODUCT REQUIREMENTS:**
-   The application must be stable and ready for production use.
-   The integration with the new  POS system must be complete, secure, and resilient to failure.
-   Each restaurant location (Rellingen, Henstedt-Ulzburg) must have its own separate configuration within the multi-tenant  system.
-   A mechanism to handle failed POS order submissions must be in place to protect revenue.

**User's preferred language**: German

**what currently exists?**
The ZOZO Burger application features a FastAPI backend and a React frontend. The admin panel has been significantly improved: product drag-and-drop reordering is implemented, data inconsistencies (duplicate products, incorrect category IDs) have been resolved, and the product editing dialog now dynamically handles multiple pricing structures.

The application is fully integrated with the new  POS system. The  connector is implemented, and the admin UI allows separate configuration (API URL, API Key) for each restaurant location, supporting the multi-tenant architecture of Cash-X. Live connections for both the Rellingen and Henstedt-Ulzburg branches have been configured with unique API keys and successfully tested. A comprehensive project summary document has been created at .

**Last working item**:
-   **Last item agent was working:** Implementing a robust POS retry mechanism to prevent lost orders. The agent has written the core logic into , which includes retry delays (exponential backoff), handling for hard/soft fails, and logic to queue terminally failed orders into a new  database collection.
-   **Status:** IN PROGRESS
-   **Agent Testing Done:** N
-   **Which testing method agent to use?** Both.
    1.  **Backend testing agent:** To create a test suite for  that simulates POS failures (e.g., by mocking the  client to raise exceptions) and verifies that orders are correctly retried and eventually queued.
    2.  **Frontend testing agent:** To test the new admin UI page for viewing and manually retrying failed orders.
-   **User Testing Done:** N

**All Pending/In progress Issue list**:
-   None. All previously reported bugs and issues have been resolved. The only pending item is the task currently in progress.

**Issues Detail:**
-   N/A

**In progress Task List**:
-   **Task 1: Complete POS Retry Mechanism Implementation (P0)**

**Task Detail:**
-   **Task 1: Complete POS Retry Mechanism Implementation**
    -   **Where to resume:**
        1.  **Backend:** The logic in  is written. Now, create the API endpoints in  for the admin UI:
            -   : To list all queued orders.
            -   : To trigger a manual retry.
            -   Ensure these endpoints are secured and accessible only to admins (Super Admin sees all, Branch Admin sees their own location).
        2.  **Frontend:** Create a new admin page at  (route: ).
            -   This page should display a table of failed orders with details (Order #, Location, Time, Error, Retry Count).
            -   Each row should have a Retry button that calls the new backend endpoint.
            -   Display success/error toasts after a manual retry attempt.
    -   **What will be achieved with this?** This will create a resilient system that protects revenue by preventing paid orders from being lost due to temporary POS API outages or network issues. It provides admins with visibility and control over failed submissions.
    -   **Status:** IN PROGRESS
    -   **Should Test frontend/backend/both after fix?** Both.
    -   **Blocked on something:** Nothing.

**Upcoming and Future Tasks**
-   None. All user requests have been addressed or are currently in progress. The user has been provided with prompts to continue development on the separate Cash-X system with another agent.

**Completed work in this session**
-   **Drag & Drop Product Reordering:** Implemented a drag-and-drop interface in the admin product list () and the corresponding backend endpoint () to save the new order.
-   **Database & UI Cleanup:**
    -   Fixed an issue where the admin product table displayed Category IDs instead of names by correcting data inconsistencies for 112 products.
    -   Identified and deleted 111 duplicate products from the database, reducing the product count from 276 to 164.
-   **Dynamic Product Pricing:** Updated the product creation/editing modal () to correctly display price fields based on product type (e.g., one price for special burgers, two sizes for standard burgers, corrected Pizza sizes).
-   **Cash-X POS Integration (Full Lifecycle):**
    -   Proposed and defined an API specification for the user's custom-built Cash-X system.
    -   Implemented the  connector based on the spec.
    -   Updated the admin UI () to be fully dynamic, allowing selection and configuration of Cash-X.
    -   Configured and live-tested the connection to the multi-tenant Cash-X system, setting up unique API keys for both Rellingen and Henstedt-Ulzburg locations.
-   **Generated Prompts for Cash-X System:** Created multiple detailed, copy-pasteable prompts for the user to give to a parallel agent to build the Cash-X system, including multi-tenancy, smart product auto-tagging, and a guided in-store ordering UI.
-   **Project Documentation:** Created a comprehensive project summary document at .

**Earlier issues found/mentioned but not fixed**
-   None.

**Known issue recurrence from previous fork**
-   None.

**Code Architecture**


**Key Technical Concepts**
-   **POS Integration:** Full lifecycle from API definition to multi-tenant client implementation.
-   **Resilient Systems:** Designing and implementing a POS retry mechanism with exponential backoff and a failure queue.
-   **Multi-Tenancy:** Adapting the client application (ZOZO Burger) to work with a multi-tenant SaaS backend (Cash-X) by using per-location API keys.
-   **Dynamic Frontend:** Building React components that adapt their UI based on backend data (e.g., product pricing fields, POS configuration forms).
-   **Data Cleansing:** Writing and executing scripts to fix inconsistencies and remove duplicates in a production database.
-   **Drag & Drop:** Using a library () to implement a user-friendly reordering feature.

**key DB schema**
-   **products:** Contains product details, including dynamic pricing fields (, , ).
-   **pos_configs:** Stores POS configuration (provider, API URL, API Key) on a per-location basis.
-   **failed_pos_orders (New):**
    -   , , , , , , , , .

**changes in tech stack**
-   , ,  were added to the frontend .

**All files of reference**
-   : Contains the new, critical POS retry logic.
-   : The client-side implementation for the  POS.
-   : The main admin page for products, now with drag-and-drop.
-   : The admin page for configuring POS systems for each location.
-   : The complete project documentation created in this session.

**Areas that need refactoring**:
-   **Admin Product Management UI:** The previous summary noted two components for managing products:  and . While the agent made  the primary one,  still exists. Consolidating these would reduce code duplication and potential confusion.

**key api endpoints**
-   : For saving the new product order after drag-and-drop.
-   : To save the POS configuration for a specific location.
-   : To test the POS connection.
-   **(Upcoming)** : To list queued failed orders.
-   **(Upcoming)** : To manually retry a failed order.

**Critical Info for New Agent**
-   **The POS Retry mechanism is a critical go-live feature.** The user considers it Umsatz-Schutz (revenue protection). Your immediate priority is to complete its implementation (backend API endpoints + frontend UI).
-   **The application is now architected for a multi-tenant POS.** ZOZO Burger is the client. Cash-X is the server (being built by a parallel agent). Each ZOZO location has its own API key for Cash-X, which is configured in the admin panel.
-   **User is highly engaged and driving the architecture.** The user is providing detailed requirements and managing the development of the separate Cash-X system. Be prepared for clear, specific, and technically-minded feedback.

**documents and test reports created in this job**
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User (pending):** Asked the agent to implement the POS retry mechanism immediately, confirming it's a critical go-live feature, not a nice-to-have. Provided a detailed set of requirements for the implementation.
2.  **Agent:** Provided the full content of , listed mandatory ENV variables for production, identified the top 3 go-live blockers, and gave a detailed implementation proposal for the POS retry mechanism.
3.  **User:** Asked for the full content of the summary file, mandatory ENV vars, go-live blockers, and a proposal for POS retry.
4.  **Agent:** Confirmed creation of the final project summary document.
5.  **User:** Requested a final, comprehensive project overview document () and the agent's honest recommendations for next steps.
6.  **Agent:** Generated the final, most detailed prompt for the parallel agent building Cash-X, outlining a complete guided in-store ordering system with complex business logic (upsells, free items, etc.).
7.  **User:** Added significant new requirements for the in-store ordering flow in Cash-X, including upsells for desserts, gratis items for certain categories, and paid add-ons.
8.  **Agent:** Generated a prompt for the Cash-X agent to implement a smart auto-tagging system using fuzzy matching to minimize manual product mapping.
9.  **User:** Asked if the tagging system could be smarter and automatically match similar names (e.g., Cheeseburger 125g vs. Cheeseburger Medium 125g).
10. **Agent:** Generated a prompt for the Cash-X agent to implement a flexible optional tagging system, ensuring no orders are ever blocked while still allowing for reporting.

**Project Health Check:**
-   **Broken:** None.
-   **Mocked:** The  POS connection points to a preview URL. This needs to be updated to the production URL () during deployment. The  connector is now considered a legacy fallback.

**3rd Party Integrations**
-   **MongoDB:** Primary database.
-   **FastAPI / React:** Core application frameworks.
-   **Resend:** For transactional emails.
-   **Cash-X (custom):** The primary, multi-tenant POS system. The integration is live-tested against its preview environment. Requires a Base URL and a per-location API Key.
-   **ExpertOrder (EOCloud OSP API):** Legacy POS system.

**Testing status**
-   **Testing agent used after significant changes:** NO
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None.
-   **Known regressions:** None.

**Credentials to test flow:**
-   **Super Admin:**  / 
-   **Rellingen Admin:**  / 
-   **Henstedt Admin:**  / 
-   **Cash-X API Key (Rellingen):** 
-   **Cash-X API Key (Henstedt):** 

**What agent forgot to execute**
-   The agent is in the middle of the POS retry implementation, as requested. Nothing has been forgotten.</analysis>
